import { ModalProps } from '../../Components/Overlay/Modal';
import { DataEventHandler, Children } from '../../types';
import { PropsWithChildren } from 'react';
declare const _default: (children: Children, props?: PropsWithChildren<ModalProps>) => DataEventHandler;
export default _default;
