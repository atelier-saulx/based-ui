import { DataEventHandler } from '../../../types';
import { TableitemProps } from '../types';
declare const getFieldSizes: (width: number, itemProps: TableitemProps, onOptions: DataEventHandler, large: boolean) => TableitemProps;
export default getFieldSizes;
