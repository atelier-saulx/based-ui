/// <reference types="react" />
import { TabsProps } from './types';
export declare const Tabs: ({ onChange, active, tabs, noIndicator, style, noBorder, indicatorMargin, color, }: TabsProps) => JSX.Element;
